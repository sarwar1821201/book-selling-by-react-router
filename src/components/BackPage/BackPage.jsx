import React from 'react';

const BackPage = () => {
    return (
        <div>
            <h2>This an Background Page. Its locate after home page ,that means it's states  after the header components. it has a background image </h2>
        </div>
    );
};

export default BackPage;